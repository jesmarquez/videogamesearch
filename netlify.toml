[build]
  publish = "dist/"
  
[dev]
  publish = "dist/"

# Deploy Preview context: all deploys generated from
# a pull/merge request will inherit these settings.
[context.deploy-preview]
  publish = "dist/"
  
# DONT SET DIRECTORY if you dont want to change the default netlify
# folder on deploy (netlify/functions = .netlify/functions endpoint)
# [functions]
  # directory = "netlify/functions/"